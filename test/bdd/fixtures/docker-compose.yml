#
# Copyright SecureKey Technologies Inc. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
version: '2'

services:

  orb-domain1:
    container_name: orb.domain1.com
    image: ${ORB_FIXTURE_IMAGE}:latest
    restart: always
    environment:
      - ORB_SYNC_TIMEOUT=3
      - ORB_KMS_TYPE=web
      - ORB_KMS_ENDPOINT=http://orb.kms:7878
      - LOG_LEVEL=metrics=INFO:nodeinfo=WARNING:activitypub_store=INFO:expiry-service=INFO:task-manager=INFO:watermill=INFO:DEBUG
      - VCT_ENABLED=true
      - ORB_HOST_URL=172.20.0.23:443
      - ORB_HOST_METRICS_URL=172.20.0.23:48327
      # ORB_EXTERNAL_ENDPOINT is the endpoint that external clients use to invoke services. This endpoint is used
      # to generate IDs of anchor credentials and ActivityPub objects and should be resolvable by external
      # clients. This endpoint does not (typically) target a single node in the cluster but instead, a load
      # balancer servicing multiple nodes.
      - ORB_EXTERNAL_ENDPOINT=https://orb.domain1.com
      - ORB_TLS_CERTIFICATE=/etc/orb/tls/ec-pubCert.pem
      - ORB_TLS_KEY=/etc/orb/tls/ec-key.pem
      - ORB_TLS_SYSTEMCERTPOOL=true
      - ORB_TLS_CACERTS=/etc/orb/tls/ec-cacert.pem
      - ORB_DISCOVERY_DOMAIN=shared.domain.com
      - DID_NAMESPACE=did:orb
      - ALLOWED_ORIGINS=https://orb.domain1.com,https://orb.domain2.com,ipns://k51qzi5uqu5dgkmm1afrkmex5mzpu5r774jstpxjmro6mdsaullur27nfxle1q
      - ALLOWED_ORIGINS_CACHE_EXPIRATION=1m
      # BATCH_WRITER_TIMEOUT is max wait time in-between cutting batches (defined in milliseconds)
      - BATCH_WRITER_TIMEOUT=1000
      - CAS_TYPE=${CAS_TYPE}
      - REPLICATE_LOCAL_CAS_WRITES_IN_IPFS=true
      - IPFS_URL=ipfs:5001
      - IPFS_TIMEOUT=10s
      - MQ_URL=amqp://${RABBITMQ_USERNAME}:${RABBITMQ_PASSWORD}@orb.mq.domain1.com:5672/
      # MQ_REDELIVERY_INITIAL_INTERVAL is the delay for the initial redelivery attempt (default is 2s).
      - MQ_REDELIVERY_INITIAL_INTERVAL=2s
      # MQ_REDELIVERY_MULTIPLIER is the multiplier for a redelivery attempt. For example, if set to 1.5 and
      #	the previous redelivery interval was 2s then the next redelivery interval is set 3s (default is 1.5).
      - MQ_REDELIVERY_MULTIPLIER=1.5
      # MQ_REDELIVERY_MAX_INTERVAL is the maximum delay for a redelivery (default is 30s).
      - MQ_REDELIVERY_MAX_INTERVAL=30s
      # MQ_REDELIVERY_MAX_ATTEMPTS Specifies the maximum number of redelivery attempts for a failed message (default is 20).
      - MQ_REDELIVERY_MAX_ATTEMPTS=20
      # OP_QUEUE_POOL specifies the number of subscribers that concurrently process messages in the operation queue (default 5).
      - OP_QUEUE_POOL=10
      # OP_QUEUE_TASK_MONITOR_INTERVAL specifies The interval (period) in which operation queue tasks from other servers are monitored (default is 10s).
      - OP_QUEUE_TASK_MONITOR_INTERVAL=10s
      # OP_QUEUE_TASK_EXPIRATION specifies the maximum time that an operation queue task can exist in the database before
      #	it is considered to have expired. At which point, any other server instance may delete the task and repost the
      # operations associated with the task to the queue so that they are processed by another instance (default is 30s).
      - OP_QUEUE_TASK_EXPIRATION=20s
      # OP_QUEUE_MAX_REPOSTS specifies the maximum number of times an operation may be reposted to the queue after having failed (default is 10).
      - OP_QUEUE_MAX_REPOSTS=12
      # MQ_OBSERVER_POOL specifies the number of subscribers that concurrently process messages from the observer queue (default 5).
      - MQ_OBSERVER_POOL=5
      # MQ_OUTBOX_POOL specifies the number of subscribers that concurrently process messages from the outbox queue (default 5).
      - MQ_OUTBOX_POOL=7
      # MQ_INBOX_POOL specifies the number of subscribers that concurrently process messages from the inbox queue (default 5).
      - MQ_INBOX_POOL=7
      # MQ_PUBLISHER_POOL specifies the size of a channel pool for an AMQP publisher (default 25). If set to 0 then
      #	a channel pool is not used and a new channel is opened/closed for every publish to a queue.
      - MQ_PUBLISHER_POOL=50
      # MQ_PUBLISHER_CONFIRM_DELIVERY turns on delivery confirmation of published messages (default true).
      - MQ_PUBLISHER_CONFIRM_DELIVERY=true
      - MQ_MAX_CONNECTION_CHANNELS=1500
      - CID_VERSION=${CID_VERSION_DOMAIN1}
      - ANCHOR_CREDENTIAL_ISSUER=https://orb.domain1.com
      - ANCHOR_CREDENTIAL_URL=https://orb.domain1.com/vc
      # used in case that orb server signs anchor credential (there is no local witness log)
      - ANCHOR_CREDENTIAL_DOMAIN=https://orb.domain1.com
      - DATABASE_TYPE=mongodb
      - DATABASE_URL=mongodb://mongodb.shared.com:27017
      - DATABASE_PREFIX=domain1
      - HTTP_SIGNATURES_ENABLED=true

      # ORB_AUTH_TOKENS_DEF contains the authorization definition for each of the REST endpoints. Format:
      #
      # <path-expr>|<read-token1>&<read-token2>&...>|<write-token1>&<write-token2>&...>,<path-expr> ...
      #
      # Where:
      # - path-expr contains a regular expression for a path. Path expressions are processed in the order they are specified.
      # - read-token defines a token for a read (GET) operation. If not specified then authorization is not performed.
      # - write-token defines a token for a write (POST) operation. If not specified then authorization is not performed.
      #
      # If no definition is included for an endpoint then authorization is NOT performed for that endpoint.
      #
      # Example:
      #
      # ORB_AUTH_TOKENS_DEF=/services/orb/outbox|admin&read|admin,/services/orb/.*|read&admin
      # - The client requires a 'read' or 'admin' token in order to view the outbox's contents
      # - The client requires an 'admin' token in order to post to the outbox
      # - The client requires a 'read' or 'admin' token in order to perform a GET on any endpoint starting with /services/orb/
      - ORB_AUTH_TOKENS_DEF=/services/orb/keys,/services/orb/outbox|admin&read|admin,/services/orb/inbox|admin&read|admin,/services/orb/acceptlist|admin&read|admin,/allowedorigins|admin&read|admin,/services/orb/.*|read&admin,/transactions|read&admin,/sidetree/.*/identifiers|read&admin,/sidetree/.*/operations|read&admin|admin,/cas|read&admin,/log-monitor||admin,/log||admin,/policy||admin
      # ORB_AUTH_TOKENS specifies the actual values of the tokens defined in ORB_AUTH_TOKENS_DEF.
      - ORB_AUTH_TOKENS=admin=ADMIN_TOKEN,read=READ_TOKEN
      # FOLLOW_AUTH_POLICY indicates whether a 'Follow' request is automatically accepted by this service (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - FOLLOW_AUTH_POLICY=accept-list
      # INVITE_WITNESS_AUTH_POLICY indicates whether an 'Invite' witness request is automatically accepted (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - INVITE_WITNESS_AUTH_POLICY=accept-list
      # TASK_MANAGER_CHECK_INTERVAL is the interval in which to check for scheduled tasks to be run. Note that this value is
      # merely the 'check' interval since each task has its own interval.
      # Default value: 10s.
      - TASK_MANAGER_CHECK_INTERVAL=10s
      # ANCHOR_EVENT_SYNC_INTERVAL is the interval in which anchor events are synchronized with other services that
      # we're following.
      # Default value: 1m.
      - ANCHOR_EVENT_SYNC_INTERVAL=1m
      # ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE is the minimum age of an activity to be synchronized. The activity will be processed
      # only if its age is greater than this value.
      # Default value: 1m
      - ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE=1m
      # VCT_MONITORING_INTERVAL is the interval (period) in which proofs are monitored from various VCTs that promised
      # to anchor a VC by a certain time.
      # Default value: 10s.
      - VCT_MONITORING_INTERVAL=15s
      # ACTIVITYPUB_CLIENT_CACHE_SIZE sets the maximum size of an ActivityPub service and public key cache.
      # Default value: 100
      - ACTIVITYPUB_CLIENT_CACHE_SIZE=200
      # ACTIVITYPUB_CLIENT_CACHE_EXPIRATION sets the expiration time of an ActivityPub service and public key cache.
      # Default value: 1h (one hour)
      - ACTIVITYPUB_CLIENT_CACHE_EXPIRATION=90s
      # ACTIVITYPUB_IRI_CACHE_SIZE sets the maximum size of an ActivityPub actor IRI cache.
      # Default value: 100
      - ACTIVITYPUB_IRI_CACHE_SIZE=200
      # ACTIVITYPUB_IRI_CACHE_EXPIRATION sets the expiration time of an ActivityPub actor IRI cache.
      # Default value: 1h (one hour)
      - ACTIVITYPUB_IRI_CACHE_EXPIRATION=90s
      # WITNESS_POLICY_CACHE_EXPIRATION sets the expiration time of witness policy cache.
      # Default value: 30s
      - WITNESS_POLICY_CACHE_EXPIRATION=10s
      - ANCHOR_DATA_URI_MEDIA_TYPE=application/gzip;base64
      - INCLUDE_PUBLISHED_OPERATIONS_IN_METADATA=true
      - ORB_REQUEST_TOKENS=vct-read=vctread,vct-write=vctwrite
      - VCT_LOG_ENTRIES_STORE_ENABLED=true
    ports:
      - 48326:443
      - 48327:48327
      - 443:443
    command: start
    volumes:
      - ./keys/tls:/etc/orb/tls
      - ./testdata/keys/domain1:/etc/orb/activitypub
    depends_on:
      - ipfs
      - orb.kms
      - mongodb.shared.com
      - orb.mq.domain1.com
    networks:
      orb_net:
        ipv4_address: 172.20.0.23

  orb2-domain1:
    container_name: orb2.domain1.com
    image: ${ORB_FIXTURE_IMAGE}:latest
    restart: always
    environment:
      - ORB_SYNC_TIMEOUT=3
      - ORB_KMS_TYPE=web
      - ORB_KMS_ENDPOINT=http://orb.kms:7878
      - LOG_LEVEL=metrics=INFO:nodeinfo=WARNING:activitypub_store=INFO:expiry-service=INFO:task-manager=INFO:watermill=INFO:DEBUG
      - VCT_ENABLED=true
      - ORB_HOST_URL=172.20.0.2:443
      - ORB_HOST_METRICS_URL=172.20.0.2:48527
      # add delay for starting additional servers within same domain (in seconds)
      - ORB_STARTUP_DELAY=5
      # ORB_EXTERNAL_ENDPOINT is the endpoint that external clients use to invoke services. This endpoint is used
      # to generate IDs of anchor credentials and ActivityPub objects and should be resolvable by external
      # clients. This endpoint does not (typically) target a single node in the cluster but instead, a load
      # balancer servicing multiple nodes.
      - ORB_EXTERNAL_ENDPOINT=https://orb.domain1.com
      - ORB_TLS_CERTIFICATE=/etc/orb/tls/ec-pubCert.pem
      - ORB_TLS_KEY=/etc/orb/tls/ec-key.pem
      - ORB_TLS_SYSTEMCERTPOOL=true
      - ORB_TLS_CACERTS=/etc/orb/tls/ec-cacert.pem
      - ORB_DISCOVERY_DOMAIN=shared.domain.com
      - DID_NAMESPACE=did:orb
      - ALLOWED_ORIGINS=https://orb.domain1.com,https://orb.domain2.com,ipns://k51qzi5uqu5dgkmm1afrkmex5mzpu5r774jstpxjmro6mdsaullur27nfxle1q
      - ALLOWED_ORIGINS_CACHE_EXPIRATION=1m
      # BATCH_WRITER_TIMEOUT is max wait time in-between cutting batches (defined in milliseconds)
      - BATCH_WRITER_TIMEOUT=1000
      - CAS_TYPE=${CAS_TYPE}
      - REPLICATE_LOCAL_CAS_WRITES_IN_IPFS=true
      - IPFS_URL=ipfs:5001
      - IPFS_TIMEOUT=10s
      - MQ_URL=amqp://${RABBITMQ_USERNAME}:${RABBITMQ_PASSWORD}@orb.mq.domain1.com:5672/
      # OP_QUEUE_POOL specifies the number of subscribers that concurrently process messages in the operation queue (default 5).
      - OP_QUEUE_POOL=10
      # OP_QUEUE_TASK_MONITOR_INTERVAL specifies The interval (period) in which operation queue tasks from other servers are monitored (default is 10s).
      - OP_QUEUE_TASK_MONITOR_INTERVAL=10s
      # OP_QUEUE_TASK_EXPIRATION specifies the maximum time that an operation queue task can exist in the database before
      #	it is considered to have expired. At which point, any other server instance may delete the task and repost the
      # operations associated with the task to the queue so that they are processed by another instance (default is 30s).
      - OP_QUEUE_TASK_EXPIRATION=20s
      # OP_QUEUE_MAX_REPOSTS specifies the maximum number of times an operation may be reposted to the queue after having failed (default is 10).
      - OP_QUEUE_MAX_REPOSTS=12
      # MQ_OBSERVER_POOL specifies the number of subscribers that concurrently process messages from the observer queue (default 5).
      - MQ_OBSERVER_POOL=5
      # MQ_PUBLISHER_POOL specifies the size of a channel pool for an AMQP publisher (default 25). If set to 0 then
      #	a channel pool is not used and a new channel is opened/closed for every publish to a queue.
      - MQ_PUBLISHER_POOL=50
      - MQ_MAX_CONNECTION_CHANNELS=1500
      - CID_VERSION=${CID_VERSION_DOMAIN1}
      - ANCHOR_CREDENTIAL_ISSUER=https://orb2.domain1.com
      - ANCHOR_CREDENTIAL_URL=https://orb2.domain1.com/vc
      # used in case that orb server signs anchor credential (there is no local witness log)
      - ANCHOR_CREDENTIAL_DOMAIN=https://orb.domain1.com
      - DATABASE_TYPE=mongodb
      - DATABASE_URL=mongodb://mongodb.shared.com:27017
      - DATABASE_PREFIX=domain1
      - HTTP_SIGNATURES_ENABLED=true

      # ORB_AUTH_TOKENS_DEF contains the authorization definition for each of the REST endpoints. Format:
      #
      # <path-expr>|<read-token1>&<read-token2>&...>|<write-token1>&<write-token2>&...>,<path-expr> ...
      #
      # Where:
      # - path-expr contains a regular expression for a path. Path expressions are processed in the order they are specified.
      # - read-token defines a token for a read (GET) operation. If not specified then authorization is not performed.
      # - write-token defines a token for a write (POST) operation. If not specified then authorization is not performed.
      #
      # If no definition is included for an endpoint then authorization is NOT performed for that endpoint.
      #
      # Example:
      #
      # ORB_AUTH_TOKENS_DEF=/services/orb/outbox|admin&read|admin,/services/orb/.*|read&admin
      # - The client requires a 'read' or 'admin' token in order to view the outbox's contents
      # - The client requires an 'admin' token in order to post to the outbox
      # - The client requires a 'read' or 'admin' token in order to perform a GET on any endpoint starting with /services/orb/
      - ORB_AUTH_TOKENS_DEF=/services/orb/keys,/services/orb/outbox|admin&read|admin,/services/orb/inbox|admin&read|admin,/services/orb/acceptlist|admin&read|admin,/allowedorigins|admin&read|admin,/services/orb/.*|read&admin,/transactions|read&admin,/sidetree/.*/identifiers|read&admin,/sidetree/.*/operations|read&admin|admin,/cas|read&admin,/log-monitor||admin,/log||admin,/policy||admin
      # ORB_AUTH_TOKENS specifies the actual values of the tokens defined in ORB_AUTH_TOKENS_DEF.
      - ORB_AUTH_TOKENS=admin=ADMIN_TOKEN,read=READ_TOKEN
      # FOLLOW_AUTH_POLICY indicates whether a 'Follow' request is automatically accepted by this service (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - FOLLOW_AUTH_POLICY=accept-list
      # INVITE_WITNESS_AUTH_POLICY indicates whether an 'Invite' witness request is automatically accepted (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - INVITE_WITNESS_AUTH_POLICY=accept-list
      # TASK_MANAGER_CHECK_INTERVAL is the interval in which to check for scheduled tasks to be run. Note that this value is
      # merely the 'check' interval since each task has its own interval.
      # Default value: 10s.
      - TASK_MANAGER_CHECK_INTERVAL=10s
      # ANCHOR_EVENT_SYNC_INTERVAL is the interval in which anchor events are synchronized with other services that
      # we're following.
      # Default value: 1m.
      - ANCHOR_EVENT_SYNC_INTERVAL=1m
      # ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE is the minimum age of an activity to be synchronized. The activity will be processed
      # only if its age is greater than this value.
      # Default value: 1m
      - ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE=1m
      # VCT_MONITORING_INTERVAL is the interval (period) in which proofs are monitored from various VCTs that promised
      # to anchor a VC by a certain time.
      # Default value: 10s.
      - VCT_MONITORING_INTERVAL=15s
      # ACTIVITYPUB_CLIENT_CACHE_SIZE sets the maximum size of an ActivityPub service and public key cache.
      # Default value: 100
      - ACTIVITYPUB_CLIENT_CACHE_SIZE=200
      # ACTIVITYPUB_CLIENT_CACHE_EXPIRATION sets the expiration time of an ActivityPub service and public key cache.
      # Default value: 1h (one hour)
      - ACTIVITYPUB_CLIENT_CACHE_EXPIRATION=90s
      # ACTIVITYPUB_IRI_CACHE_SIZE sets the maximum size of an ActivityPub actor IRI cache.
      # Default value: 100
      - ACTIVITYPUB_IRI_CACHE_SIZE=200
      # ACTIVITYPUB_IRI_CACHE_EXPIRATION sets the expiration time of an ActivityPub actor IRI cache.
      # Default value: 1h (one hour)
      - ACTIVITYPUB_IRI_CACHE_EXPIRATION=90s
      - ANCHOR_DATA_URI_MEDIA_TYPE=application/gzip;base64
      - INCLUDE_PUBLISHED_OPERATIONS_IN_METADATA=true
      - ORB_REQUEST_TOKENS=vct-read=vctread,vct-write=vctwrite
      - VCT_LOG_ENTRIES_STORE_ENABLED=true
    ports:
      - 48526:443
      - 48527:48527
    command: start
    volumes:
      - ./keys/tls:/etc/orb/tls
      - ./testdata/keys/domain1:/etc/orb/activitypub
    depends_on:
      - ipfs
      - orb.kms
      - mongodb.shared.com
      - orb-domain1
      - orb.mq.domain1.com
    networks:
      orb_net:
        ipv4_address: 172.20.0.2

  orb-domain2:
    container_name: orb.domain2.com
    image: nginx:latest
    restart: always
    ports:
      - 48426:443
    volumes:
      - ./nginx-config/nginx.conf:/etc/nginx/nginx.conf
      - ./keys/tls:/etc/tls
    depends_on:
      - orb-domain2.backend
      - orb1-domain2.backend
    networks:
      orb_net:
        ipv4_address: 172.20.0.3

  orb-domain2.backend:
    container_name: orb-domain2.backend
    image: ${ORB_FIXTURE_IMAGE}:latest
    restart: always
    environment:
      - ORB_SYNC_TIMEOUT=3
      - ORB_KMS_TYPE=web
      - ORB_KMS_ENDPOINT=http://orb.kms:7878
      - LOG_LEVEL=metrics=INFO:nodeinfo=WARNING:activitypub_store=INFO:expiry-service=INFO:task-manager=INFO:watermill=INFO:DEBUG
      - ORB_HOST_URL=172.20.0.4:80
      - ORB_HOST_METRICS_URL=172.20.0.4:48827
      - ORB_TLS_SYSTEMCERTPOOL=true
      - ORB_TLS_CACERTS=/etc/orb/tls/ec-cacert.pem
      # ORB_EXTERNAL_ENDPOINT is the endpoint that external clients use to invoke services. This endpoint is used
      # to generate IDs of anchor credentials and ActivityPub objects and should be resolvable by external
      # clients. This endpoint does not (typically) target a single node in the cluster but instead, a load
      # balancer servicing multiple nodes.
      - ORB_EXTERNAL_ENDPOINT=https://orb.domain2.com
      - ORB_DISCOVERY_DOMAIN=shared.domain.com
      - DID_NAMESPACE=did:orb
      - ALLOWED_ORIGINS=https://orb.domain1.com,https://orb.domain2.com,ipns://k51qzi5uqu5dgkmm1afrkmex5mzpu5r774jstpxjmro6mdsaullur27nfxle1q
      # BATCH_WRITER_TIMEOUT is max wait time in-between cutting batches (defined in milliseconds)
      - BATCH_WRITER_TIMEOUT=1000
      - CAS_TYPE=${CAS_TYPE}
      - IPFS_URL=ipfs:5001
      - IPFS_TIMEOUT=10s
      - MQ_URL=amqp://${RABBITMQ_USERNAME}:${RABBITMQ_PASSWORD}@orb.mq.domain2.com:5672/
      # OP_QUEUE_POOL specifies the number of subscribers that concurrently process messages in the operation queue (default 5).
      # - OP_QUEUE_POOL=5
      # MQ_OBSERVER_POOL specifies the number of subscribers that concurrently process messages from the observer queue (default 5).
      - MQ_OBSERVER_POOL=5
      # MQ_PUBLISHER_POOL specifies the size of a channel pool for an AMQP publisher (default 25). If set to 0 then
      #	a channel pool is not used and a new channel is opened/closed for every publish to a queue.
      - MQ_PUBLISHER_POOL=25
      - MQ_MAX_CONNECTION_CHANNELS=1500
      - CID_VERSION=${CID_VERSION_DOMAIN2}
      - ANCHOR_CREDENTIAL_ISSUER=https://orb.domain2.com
      - ANCHOR_CREDENTIAL_URL=https://orb.domain2.com/vc
      # used in case that orb server signs anchor credential (there is no local witness log)
      - ANCHOR_CREDENTIAL_DOMAIN=https://orb.domain2.com
      - DATABASE_TYPE=mongodb
      - DATABASE_URL=mongodb://mongodb.shared.com:27017
      - DATABASE_PREFIX=domain2
      - HTTP_SIGNATURES_ENABLED=true

      # ORB_AUTH_TOKENS_DEF contains the authorization definition for each of the REST endpoints. Format:
      #
      # <path-expr>|<read-token1>&<read-token2>&...>|<write-token1>&<write-token2>&...>,<path-expr> ...
      #
      # Where:
      # - path-expr contains a regular expression for a path. Path expressions are processed in the order they are specified.
      # - read-token defines a token for a read (GET) operation. If not specified then authorization is not performed.
      # - write-token defines a token for a write (POST) operation. If not specified then authorization is not performed.
      #
      # If no definition is included for an endpoint then authorization is NOT performed for that endpoint.
      #
      # Example:
      #
      # ORB_AUTH_TOKENS_DEF=/services/orb/outbox|admin&read|admin,/services/orb/.*|read&admin
      # - The client requires a 'read' or 'admin' token in order to view the outbox's contents
      # - The client requires an 'admin' token in order to post to the outbox
      # - The client requires a 'read' or 'admin' token in order to perform a GET on any endpoint starting with /services/orb/
      - ORB_AUTH_TOKENS_DEF=/services/orb/keys,/services/orb/outbox|admin&read|admin,/services/orb/inbox|admin&read|admin,/services/orb/.*|read&admin,/transactions|read&admin,/sidetree/.*/identifiers|read&admin,/sidetree/.*/operations|read&admin|admin,/cas|read&admin,/log-monitor||admin,/log||admin,/policy||admin
      # ORB_AUTH_TOKENS specifies the actual values of the tokens defined in ORB_AUTH_TOKENS_DEF.
      - ORB_AUTH_TOKENS=admin=ADMIN_TOKEN,read=READ_TOKEN
      # FOLLOW_AUTH_POLICY indicates whether a 'Follow' request is automatically accepted by this service (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - FOLLOW_AUTH_POLICY=accept-list
      # INVITE_WITNESS_AUTH_POLICY indicates whether an 'Invite' witness request is automatically accepted (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - INVITE_WITNESS_AUTH_POLICY=accept-list
      # TASK_MANAGER_CHECK_INTERVAL is the interval in which to check for scheduled tasks to be run. Note that this value is
      # merely the 'check' interval since each task has its own interval.
      # Default value: 10s.
      - TASK_MANAGER_CHECK_INTERVAL=10s
      # ANCHOR_EVENT_SYNC_INTERVAL is the interval in which anchor events are synchronized with other services that
      # we're following.
      # Default value: 1m.
      - ANCHOR_EVENT_SYNC_INTERVAL=1m
      # ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE is the minimum age of an activity to be synchronized. The activity will be processed
      # only if its age is greater than this value.
      # Default value: 1m
      - ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE=1m
      - ANCHOR_DATA_URI_MEDIA_TYPE=application/json
      - ORB_REQUEST_TOKENS=vct-read=vctread,vct-write=vctwrite
    ports:
      - 48826:80
      - 48827:48827
    command: start
    volumes:
      - ./keys/tls:/etc/orb/tls
      - ./testdata/keys/domain2:/etc/orb/activitypub
    depends_on:
      - ipfs
      - orb.kms
      - mongodb.shared.com
      - orb.mq.domain2.com
    networks:
      orb_net:
        ipv4_address: 172.20.0.4

  orb1-domain2.backend:
    container_name: orb1-domain2.backend
    image: ${ORB_FIXTURE_IMAGE}:latest
    restart: always
    environment:
      - ORB_SYNC_TIMEOUT=3
      - ORB_KMS_TYPE=web
      - ORB_KMS_ENDPOINT=http://orb.kms:7878
      - LOG_LEVEL=metrics=INFO:nodeinfo=WARNING:activitypub_store=INFO:expiry-service=INFO:task-manager=INFO:watermill=INFO:DEBUG
      - ORB_HOST_URL=172.20.0.5:80
      - ORB_HOST_METRICS_URL=172.20.0.5:48927
      - ORB_TLS_SYSTEMCERTPOOL=true
      - ORB_TLS_CACERTS=/etc/orb/tls/ec-cacert.pem
      # ORB_EXTERNAL_ENDPOINT is the endpoint that external clients use to invoke services. This endpoint is used
      # to generate IDs of anchor credentials and ActivityPub objects and should be resolvable by external
      # clients. This endpoint does not (typically) target a single node in the cluster but instead, a load
      # balancer servicing multiple nodes.
      - ORB_EXTERNAL_ENDPOINT=https://orb.domain2.com
      - ORB_DISCOVERY_DOMAIN=shared.domain.com
      - DID_NAMESPACE=did:orb
      - ALLOWED_ORIGINS=https://orb.domain1.com,https://orb.domain2.com,ipns://k51qzi5uqu5dgkmm1afrkmex5mzpu5r774jstpxjmro6mdsaullur27nfxle1q
      # BATCH_WRITER_TIMEOUT is max wait time in-between cutting batches (defined in milliseconds)
      - BATCH_WRITER_TIMEOUT=1000
      - CAS_TYPE=${CAS_TYPE}
      - IPFS_URL=ipfs:5001
      - IPFS_TIMEOUT=10s
      - MQ_URL=amqp://${RABBITMQ_USERNAME}:${RABBITMQ_PASSWORD}@orb.mq.domain2.com:5672/
      # OP_QUEUE_POOL specifies the number of subscribers that concurrently process messages in the operation queue (default 5).
      # - OP_QUEUE_POOL=5
      # MQ_OBSERVER_POOL specifies the number of subscribers that concurrently process messages from the observer queue (default 5).
      - MQ_OBSERVER_POOL=5
      # MQ_PUBLISHER_POOL specifies the size of a channel pool for an AMQP publisher (default 25). If set to 0 then
      #	a channel pool is not used and a new channel is opened/closed for every publish to a queue.
      - MQ_PUBLISHER_POOL=25
      - MQ_MAX_CONNECTION_CHANNELS=1500
      - CID_VERSION=${CID_VERSION_DOMAIN2}
      - ANCHOR_CREDENTIAL_ISSUER=https://orb.domain2.com
      - ANCHOR_CREDENTIAL_URL=https://orb.domain2.com/vc
      # used in case that orb server signs anchor credential (there is no local witness log)
      - ANCHOR_CREDENTIAL_DOMAIN=https://orb.domain2.com
      - DATABASE_TYPE=mongodb
      - DATABASE_URL=mongodb://mongodb.shared.com:27017
      - DATABASE_PREFIX=domain2
      - HTTP_SIGNATURES_ENABLED=true

      # ORB_AUTH_TOKENS_DEF contains the authorization definition for each of the REST endpoints. Format:
      #
      # <path-expr>|<read-token1>&<read-token2>&...>|<write-token1>&<write-token2>&...>,<path-expr> ...
      #
      # Where:
      # - path-expr contains a regular expression for a path. Path expressions are processed in the order they are specified.
      # - read-token defines a token for a read (GET) operation. If not specified then authorization is not performed.
      # - write-token defines a token for a write (POST) operation. If not specified then authorization is not performed.
      #
      # If no definition is included for an endpoint then authorization is NOT performed for that endpoint.
      #
      # Example:
      #
      # ORB_AUTH_TOKENS_DEF=/services/orb/outbox|admin&read|admin,/services/orb/.*|read&admin
      # - The client requires a 'read' or 'admin' token in order to view the outbox's contents
      # - The client requires an 'admin' token in order to post to the outbox
      # - The client requires a 'read' or 'admin' token in order to perform a GET on any endpoint starting with /services/orb/
      - ORB_AUTH_TOKENS_DEF=/services/orb/keys,/services/orb/outbox|admin&read|admin,/services/orb/inbox|admin&read|admin,/services/orb/.*|read&admin,/transactions|read&admin,/sidetree/.*/identifiers|read&admin,/sidetree/.*/operations|read&admin|admin,/cas|read&admin,/log-monitor||admin,/log||admin,/policy||admin
      # ORB_AUTH_TOKENS specifies the actual values of the tokens defined in ORB_AUTH_TOKENS_DEF.
      - ORB_AUTH_TOKENS=admin=ADMIN_TOKEN,read=READ_TOKEN
      # FOLLOW_AUTH_POLICY indicates whether a 'Follow' request is automatically accepted by this service (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - FOLLOW_AUTH_POLICY=accept-list
      # INVITE_WITNESS_AUTH_POLICY indicates whether an 'Invite' witness request is automatically accepted (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - INVITE_WITNESS_AUTH_POLICY=accept-list
      # TASK_MANAGER_CHECK_INTERVAL is the interval in which to check for scheduled tasks to be run. Note that this value is
      # merely the 'check' interval since each task has its own interval.
      # Default value: 10s.
      - TASK_MANAGER_CHECK_INTERVAL=10s
      # ANCHOR_EVENT_SYNC_INTERVAL is the interval in which anchor events are synchronized with other services that
      # we're following.
      # Default value: 1m.
      - ANCHOR_EVENT_SYNC_INTERVAL=1m
      # ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE is the minimum age of an activity to be synchronized. The activity will be processed
      # only if its age is greater than this value.
      # Default value: 1m
      - ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE=1m
      - ANCHOR_DATA_URI_MEDIA_TYPE=application/json
      - ORB_REQUEST_TOKENS=vct-read=vctread,vct-write=vctwrite
    ports:
      - 48926:80
      - 48927:48927
    command: start
    volumes:
      - ./keys/tls:/etc/orb/tls
      - ./testdata/keys/domain2:/etc/orb/activitypub
    depends_on:
      - ipfs
      - orb.kms
      - mongodb.shared.com
      - orb.mq.domain2.com
    networks:
      orb_net:
        ipv4_address: 172.20.0.5

  orb-domain3:
    container_name: orb.domain3.com
    image: ${ORB_FIXTURE_IMAGE}:latest
    restart: always
    environment:
      - ORB_SYNC_TIMEOUT=3
      - ORB_KMS_TYPE=web
      - ORB_KMS_ENDPOINT=http://orb.kms:7878
      - LOG_LEVEL=metrics=INFO:nodeinfo=WARNING:activitypub_store=INFO:expiry-service=INFO:task-manager=INFO:watermill=INFO:DEBUG
      - VCT_ENABLED=true
      - ORB_HOST_URL=172.20.0.6:443
      - ORB_HOST_METRICS_URL=172.20.0.6:48627
      # ORB_EXTERNAL_ENDPOINT is the endpoint that external clients use to invoke services. This endpoint is used
      # to generate IDs of anchor credentials and ActivityPub objects and should be resolvable by external
      # clients. This endpoint does not (typically) target a single node in the cluster but instead, a load
      # balancer servicing multiple nodes.
      - ORB_EXTERNAL_ENDPOINT=https://orb.domain3.com
      - ORB_TLS_CERTIFICATE=/etc/orb/tls/ec-pubCert.pem
      - ORB_TLS_KEY=/etc/orb/tls/ec-key.pem
      - ORB_TLS_SYSTEMCERTPOOL=true
      - ORB_TLS_CACERTS=/etc/orb/tls/ec-cacert.pem
      - ORB_DISCOVERY_DOMAIN=shared.domain.com
      - DID_NAMESPACE=did:orb
      - ALLOWED_ORIGINS=https://orb.domain1.com,https://orb.domain2.com,https://orb.domain3.com,ipns://k51qzi5uqu5dgkmm1afrkmex5mzpu5r774jstpxjmro6mdsaullur27nfxle1q
      # BATCH_WRITER_TIMEOUT is max wait time in-between cutting batches (defined in milliseconds)
      - BATCH_WRITER_TIMEOUT=2000
      - CAS_TYPE=${CAS_TYPE}
      - IPFS_URL=ipfs:5001
      - IPFS_TIMEOUT=10s
      - MQ_URL=amqp://${RABBITMQ_USERNAME}:${RABBITMQ_PASSWORD}@orb.mq.domain3.com:5672/
      # OP_QUEUE_POOL specifies the number of subscribers that concurrently process messages in the operation queue (default 5).
      # - OP_QUEUE_POOL=5
      # MQ_OBSERVER_POOL specifies the number of subscribers that concurrently process messages from the observer queue (default 5).
      - MQ_OBSERVER_POOL=5
      # MQ_PUBLISHER_POOL specifies the size of a channel pool for an AMQP publisher (default 25). If set to 0 then
      #	a channel pool is not used and a new channel is opened/closed for every publish to a queue.
      - MQ_PUBLISHER_POOL=25
      - MQ_MAX_CONNECTION_CHANNELS=1500
      - CID_VERSION=${CID_VERSION_DOMAIN3}
      - ANCHOR_CREDENTIAL_ISSUER=https://orb.domain3.com
      - ANCHOR_CREDENTIAL_URL=https://orb.domain3.com/vc
      # used in case that orb server signs anchor credential (there is no local witness log)
      - ANCHOR_CREDENTIAL_DOMAIN=https://orb.domain3.com
      - DATABASE_TYPE=mongodb
      - DATABASE_URL=mongodb://mongodb.shared.com:27017
      - DATABASE_PREFIX=domain3
      - HTTP_SIGNATURES_ENABLED=true
      - UNPUBLISHED_OPERATION_STORE_ENABLED=true
      - UNPUBLISHED_OPERATION_STORE_OPERATION_TYPES=update
      - INCLUDE_UNPUBLISHED_OPERATIONS_IN_METADATA=true
      - INCLUDE_PUBLISHED_OPERATIONS_IN_METADATA=true
      - RESOLVE_FROM_ANCHOR_ORIGIN=true
      - VERIFY_LATEST_FROM_ANCHOR_ORIGIN=true

      # ORB_AUTH_TOKENS_DEF contains the authorization definition for each of the REST endpoints. Format:
      #
      # <path-expr>|<read-token1>&<read-token2>&...>|<write-token1>&<write-token2>&...>,<path-expr> ...
      #
      # Where:
      # - path-expr contains a regular expression for a path. Path expressions are processed in the order they are specified.
      # - read-token defines a token for a read (GET) operation. If not specified then authorization is not performed.
      # - write-token defines a token for a write (POST) operation. If not specified then authorization is not performed.
      #
      # If no definition is included for an endpoint then authorization is NOT performed for that endpoint.
      #
      # Example:
      #
      # ORB_AUTH_TOKENS_DEF=/services/orb/outbox|admin&read|admin,/services/orb/.*|read&admin
      # - The client requires a 'read' or 'admin' token in order to view the outbox's contents
      # - The client requires an 'admin' token in order to post to the outbox
      # - The client requires a 'read' or 'admin' token in order to perform a GET on any endpoint starting with /services/orb/
      - ORB_AUTH_TOKENS_DEF=/services/orb/outbox||admin,/services/orb/inbox||admin,/sidetree/.*/operations||admin,/log-monitor||admin,/log||admin,/policy||admin
      # ORB_AUTH_TOKENS specifies the actual values of the tokens defined in ORB_AUTH_TOKENS_DEF.
      - ORB_AUTH_TOKENS=admin=ADMIN_TOKEN
      # ORB_CLIENT_AUTH_TOKENS_DEF follows the same rules as ORB_AUTH_TOKENS_DEF but is used by the Orb client transport to
      # determine whether an HTTP signature is required for an outbound HTTP request. If not specified then it is assumed
      # to be the same as ORB_AUTH_TOKENS_DEF.
      - ORB_CLIENT_AUTH_TOKENS_DEF=/services/orb/keys,/services/orb/outbox|admin&read|admin,/services/orb/inbox|admin&read|admin,/services/orb/acceptlist|admin&read|admin,/allowedorigins|admin&read|admin,/services/orb/.*|read&admin,/transactions|read&admin,/sidetree/.*/identifiers|read&admin,/sidetree/.*/operations|read&admin|admin,/cas|read&admin
      # ORB_CLIENT_AUTH_TOKENS specifies the actual values of the tokens defined in ORB_CLIENT_AUTH_TOKENS_DEF. If not specified
      # then it is assumed to be the same as ORB_AUTH_TOKENS.
      - ORB_CLIENT_AUTH_TOKENS=admin=ADMIN_TOKEN,read=READ_TOKEN
      # FOLLOW_AUTH_POLICY indicates whether a 'Follow' request is automatically accepted by this service (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - FOLLOW_AUTH_POLICY=accept-all
      # INVITE_WITNESS_AUTH_POLICY indicates whether an 'Invite' witness request is automatically accepted (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - INVITE_WITNESS_AUTH_POLICY=accept-all
      # TASK_MANAGER_CHECK_INTERVAL is the interval in which to check for scheduled tasks to be run. Note that this value is
      # merely the 'check' interval since each task has its own interval.
      # Default value: 10s.
      - TASK_MANAGER_CHECK_INTERVAL=10s
      # ANCHOR_EVENT_SYNC_INTERVAL is the interval in which anchor events are synchronized with other services that
      # we're following.
      # Default value: 1m.
      - ANCHOR_EVENT_SYNC_INTERVAL=1m
      # ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE is the minimum age of an activity to be synchronized. The activity will be processed
      # only if its age is greater than this value.
      # Default value: 1m
      - ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE=1m
      - ORB_REQUEST_TOKENS=vct-read=vctread,vct-write=vctwrite
      - VCT_LOG_ENTRIES_STORE_ENABLED=true
    ports:
      - 48626:443
      - 48627:48627
    command: start
    volumes:
      - ./keys/tls:/etc/orb/tls
      - ./testdata/keys/domain3:/etc/orb/activitypub
    depends_on:
      - ipfs
      - orb.kms
      - mongodb.shared.com
      - orb.mq.domain3.com
    networks:
      orb_net:
        ipv4_address: 172.20.0.6

  orb-domain4:
    container_name: orb.domain4.com
    image: ${ORB_FIXTURE_IMAGE}:latest
    restart: always
    environment:
      - ORB_SYNC_TIMEOUT=3
      - ORB_KMS_TYPE=aws
      - ORB_KMS_ENDPOINT=http://aws-kms:8020
      - ORB_VC_SIGN_KEYS_ID=aws-kms://arn:aws:kms:ca-central-1:111122223333:alias/vc-sign
      - ORB_VC_SIGN_ACTIVE_KEY_ID=aws-kms://arn:aws:kms:ca-central-1:111122223333:alias/vc-sign
      - ORB_HTTP_SIGN_ACTIVE_KEY_ID=aws-kms://arn:aws:kms:ca-central-1:111122223333:alias/http-sign
      - AWS_ACCESS_KEY_ID=mock
      - AWS_SECRET_ACCESS_KEY=mock
      - LOG_LEVEL=metrics=INFO:nodeinfo=WARNING:activitypub_store=INFO:expiry-service=INFO:task-manager=INFO:watermill=INFO:DEBUG
      - ORB_HOST_URL=172.20.0.7:443
      - ORB_HOST_METRICS_URL=172.20.0.7:48727
      # ORB_EXTERNAL_ENDPOINT is the endpoint that external clients use to invoke services. This endpoint is used
      # to generate IDs of anchor credentials and ActivityPub objects and should be resolvable by external
      # clients. This endpoint does not (typically) target a single node in the cluster but instead, a load
      # balancer servicing multiple nodes.
      - ORB_EXTERNAL_ENDPOINT=https://orb.domain4.com
      - ORB_TLS_CERTIFICATE=/etc/orb/tls/ec-pubCert.pem
      - ORB_TLS_KEY=/etc/orb/tls/ec-key.pem
      - ORB_TLS_SYSTEMCERTPOOL=true
      - ORB_TLS_CACERTS=/etc/orb/tls/ec-cacert.pem
      - ORB_DISCOVERY_DOMAIN=shared.domain.com
      - DID_NAMESPACE=did:orb
      - DID_DISCOVERY_ENABLED=true
      - ALLOWED_ORIGINS=https://orb.domain4.com,https://orb.domain1.com,ipns://k51qzi5uqu5dgkmm1afrkmex5mzpu5r774jstpxjmro6mdsaullur27nfxle1q
      # BATCH_WRITER_TIMEOUT is max wait time in-between cutting batches (defined in milliseconds)
      - BATCH_WRITER_TIMEOUT=5000
      - CAS_TYPE=${CAS_TYPE}
      - IPFS_URL=ipfs:5001
      - IPFS_TIMEOUT=10s
      - MQ_URL=amqp://${RABBITMQ_USERNAME}:${RABBITMQ_PASSWORD}@orb.mq.domain4.com:5672/
      # OP_QUEUE_POOL specifies the number of subscribers that concurrently process messages in the operation queue (default 5).
      # - OP_QUEUE_POOL=5
      # MQ_OBSERVER_POOL specifies the number of subscribers that concurrently process messages from the observer queue (default 5).
      - MQ_OBSERVER_POOL=3
      # MQ_PUBLISHER_POOL specifies the size of a channel pool for an AMQP publisher (default 25). If set to 0 then
      #	a channel pool is not used and a new channel is opened/closed for every publish to a queue.
      - MQ_PUBLISHER_POOL=25
      - MQ_MAX_CONNECTION_CHANNELS=1500
      - CID_VERSION=${CID_VERSION_DOMAIN4}
      - ANCHOR_CREDENTIAL_ISSUER=https://orb.domain4.com
      - ANCHOR_CREDENTIAL_URL=https://orb.domain4.com/vc
      # used in case that orb server signs anchor credential (there is no local witness log)
      - ANCHOR_CREDENTIAL_DOMAIN=https://orb.domain4.com
      - DATABASE_TYPE=mongodb
      - DATABASE_URL=mongodb://mongodb.shared.com:27017
      - DATABASE_PREFIX=domain4
      - HTTP_SIGNATURES_ENABLED=true
      - UNPUBLISHED_OPERATION_STORE_ENABLED=true
      - INCLUDE_UNPUBLISHED_OPERATIONS_IN_METADATA=true
      - INCLUDE_PUBLISHED_OPERATIONS_IN_METADATA=true
      - RESOLVE_FROM_ANCHOR_ORIGIN=true
      - VERIFY_LATEST_FROM_ANCHOR_ORIGIN=true
      - UNPUBLISHED_OPERATION_LIFETIME=30s # This is set to a somewhat low value in order to speed up one of the BDD tests which relies on it
      - DATA_EXPIRY_CHECK_INTERVAL=15s # This is set to a somewhat low value in order to speed up one of the BDD tests which relies on it
      - ANCHOR_STATUS_IN_PROCESS_GRACE_PERIOD=3s
      - ANCHOR_STATUS_MONITORING_INTERVAL=2s
      - MAX_WITNESS_DELAY=30s
      - WITNESS_STORE_EXPIRY_PERIOD=32s

      # ORB_AUTH_TOKENS_DEF contains the authorization definition for each of the REST endpoints. Format:
      #
      # <path-expr>|<read-token1>&<read-token2>&...>|<write-token1>&<write-token2>&...>,<path-expr> ...
      #
      # Where:
      # - path-expr contains a regular expression for a path. Path expressions are processed in the order they are specified.
      # - read-token defines a token for a read (GET) operation. If not specified then authorization is not performed.
      # - write-token defines a token for a write (POST) operation. If not specified then authorization is not performed.
      #
      # If no definition is included for an endpoint then authorization is NOT performed for that endpoint.
      #
      # Example:
      #
      # ORB_AUTH_TOKENS_DEF=/services/orb/outbox|admin&read|admin,/services/orb/.*|read&admin
      # - The client requires a 'read' or 'admin' token in order to view the outbox's contents
      # - The client requires an 'admin' token in order to post to the outbox
      # - The client requires a 'read' or 'admin' token in order to perform a GET on any endpoint starting with /services/orb/
      - ORB_AUTH_TOKENS_DEF=/services/orb/keys,/services/orb/outbox|admin&read|admin,/services/orb/inbox|admin&read|admin,/services/orb/acceptlist|admin&read|admin,/allowedorigins|admin&read|admin,/services/orb/.*|read&admin,/transactions|read&admin,/sidetree/.*/identifiers|read&admin,/sidetree/.*/operations|read&admin|admin,/cas|read&admin,/log-monitor||admin,/log||admin,/policy||admin
      # ORB_AUTH_TOKENS specifies the actual values of the tokens defined in ORB_AUTH_TOKENS_DEF.
      - ORB_AUTH_TOKENS=admin=ADMIN_TOKEN,read=READ_TOKEN
      # FOLLOW_AUTH_POLICY indicates whether a 'Follow' request is automatically accepted by this service (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - FOLLOW_AUTH_POLICY=accept-all
      # INVITE_WITNESS_AUTH_POLICY indicates whether an 'Invite' witness request is automatically accepted (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - INVITE_WITNESS_AUTH_POLICY=accept-all
      # TASK_MANAGER_CHECK_INTERVAL is the interval in which to check for scheduled tasks to be run. Note that this value is
      # merely the 'check' interval since each task has its own interval.
      # Default value: 10s.
      - TASK_MANAGER_CHECK_INTERVAL=2s
      # ANCHOR_EVENT_SYNC_INTERVAL is the interval in which anchor events are synchronized with other services that
      # we're following.
      # Default value: 1m.
      - ANCHOR_EVENT_SYNC_INTERVAL=1m
      # ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE is the minimum age of an activity to be synchronized. The activity will be processed
      # only if its age is greater than this value.
      # Default value: 1m
      - ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE=1m
      - ORB_REQUEST_TOKENS=vct-read=vctread,vct-write=vctwrite
    ports:
      - 48726:443
      - 48727:48727
    command: start
    volumes:
      - ./keys/tls:/etc/orb/tls
      - ./testdata/keys/domain2:/etc/orb/activitypub
    depends_on:
      - ipfs
      - orb.kms
      - mongodb.shared.com
      - orb.mq.domain4.com
    networks:
      orb_net:
        ipv4_address: 172.20.0.7

  orb-domain5:
    container_name: orb.domain5.com
    image: ${ORB_FIXTURE_IMAGE}:latest
    restart: always
    environment:
      - ORB_SYNC_TIMEOUT=3
      - ORB_KMS_TYPE=web
      - ORB_KMS_ENDPOINT=http://orb5.kms:7878
      - LOG_LEVEL=metrics=INFO:nodeinfo=WARNING:activitypub_store=INFO:expiry-service=INFO:task-manager=INFO:watermill=INFO:DEBUG
      - ORB_HOST_URL=172.20.0.31:443
      - ORB_HOST_METRICS_URL=172.20.0.31:48727
      # ORB_EXTERNAL_ENDPOINT is the endpoint that external clients use to invoke services. This endpoint is used
      # to generate IDs of anchor credentials and ActivityPub objects and should be resolvable by external
      # clients. This endpoint does not (typically) target a single node in the cluster but instead, a load
      # balancer servicing multiple nodes.
      - ORB_EXTERNAL_ENDPOINT=https://orb.domain5.com
      - ORB_TLS_CERTIFICATE=/etc/orb/tls/ec-pubCert.pem
      - ORB_TLS_KEY=/etc/orb/tls/ec-key.pem
      - ORB_TLS_SYSTEMCERTPOOL=true
      - ORB_TLS_CACERTS=/etc/orb/tls/ec-cacert.pem
      - ORB_DISCOVERY_DOMAIN=shared.domain.com
      - DID_NAMESPACE=did:orb
      - DID_DISCOVERY_ENABLED=false
      - ALLOWED_ORIGINS=https://orb.domain5.com,https://orb.domain1.com,https://orb.domain2.com,ipns://k51qzi5uqu5dgkmm1afrkmex5mzpu5r774jstpxjmro6mdsaullur27nfxle1q
      # BATCH_WRITER_TIMEOUT is max wait time in-between cutting batches (defined in milliseconds)
      - BATCH_WRITER_TIMEOUT=5000
      - CAS_TYPE=${CAS_TYPE}
      - IPFS_URL=ipfs:5001
      - IPFS_TIMEOUT=10s
      - MQ_URL=amqp://${RABBITMQ_USERNAME}:${RABBITMQ_PASSWORD}@orb.mq.domain5.com:5672/
      # OP_QUEUE_POOL specifies the number of subscribers that concurrently process messages in the operation queue (default 5).
      # - OP_QUEUE_POOL=5
      # MQ_OBSERVER_POOL specifies the number of subscribers that concurrently process messages from the observer queue (default 5).
      - MQ_OBSERVER_POOL=5
      # MQ_PUBLISHER_POOL specifies the size of a channel pool for an AMQP publisher (default 25). If set to 0 then
      #	a channel pool is not used and a new channel is opened/closed for every publish to a queue.
      - MQ_PUBLISHER_POOL=25
      - MQ_MAX_CONNECTION_CHANNELS=1500
      - CID_VERSION=${CID_VERSION_DOMAIN4}
      - ANCHOR_CREDENTIAL_ISSUER=https://orb.domain5.com
      - ANCHOR_CREDENTIAL_URL=https://orb.domain5.com/vc
      # used in case that orb server signs anchor credential (there is no local witness log)
      - ANCHOR_CREDENTIAL_DOMAIN=https://orb.domain5.com
      - DATABASE_TYPE=mongodb
      - DATABASE_URL=mongodb://mongodb.domain5.com:27017
      - HTTP_SIGNATURES_ENABLED=true
      - UNPUBLISHED_OPERATION_STORE_ENABLED=true
      # ORB_AUTH_TOKENS_DEF contains the authorization definition for each of the REST endpoints. Format:
      #
      # <path-expr>|<read-token1>&<read-token2>&...>|<write-token1>&<write-token2>&...>,<path-expr> ...
      #
      # Where:
      # - path-expr contains a regular expression for a path. Path expressions are processed in the order they are specified.
      # - read-token defines a token for a read (GET) operation. If not specified then authorization is not performed.
      # - write-token defines a token for a write (POST) operation. If not specified then authorization is not performed.
      #
      # If no definition is included for an endpoint then authorization is NOT performed for that endpoint.
      #
      # Example:
      #
      # ORB_AUTH_TOKENS_DEF=/services/orb/outbox|admin&read|admin,/services/orb/.*|read&admin
      # - The client requires a 'read' or 'admin' token in order to view the outbox's contents
      # - The client requires an 'admin' token in order to post to the outbox
      # - The client requires a 'read' or 'admin' token in order to perform a GET on any endpoint starting with /services/orb/
      #      - ORB_AUTH_TOKENS_DEF=/services/orb/keys,/services/orb/outbox|admin&read|admin,/services/orb/inbox|admin&read|admin,/services/orb/.*|read&admin,/transactions|read&admin,/sidetree/.*/identifiers|read&admin,/sidetree/.*/operations|read&admin|admin,/cas|read&admin
      # ORB_AUTH_TOKENS specifies the actual values of the tokens defined in ORB_AUTH_TOKENS_DEF.
      #      - ORB_AUTH_TOKENS=admin=ADMIN_TOKEN,read=READ_TOKEN
      # ORB_CLIENT_AUTH_TOKENS_DEF follows the same rules as ORB_AUTH_TOKENS_DEF but is used by the Orb client transport to
      # determine whether an HTTP signature is required for an outbound HTTP request. If not specified then it is assumed
      # to be the same as ORB_AUTH_TOKENS_DEF.
      - ORB_CLIENT_AUTH_TOKENS_DEF=/services/orb/keys,/services/orb/outbox|admin&read|admin,/services/orb/inbox|admin&read|admin,/services/orb/acceptlist|admin&read|admin,/allowedorigins|admin&read|admin,/services/orb/.*|read&admin,/transactions|read&admin,/sidetree/.*/identifiers|read&admin,/sidetree/.*/operations|read&admin|admin,/cas|read&admin
      # ORB_CLIENT_AUTH_TOKENS specifies the actual values of the tokens defined in ORB_CLIENT_AUTH_TOKENS_DEF. If not specified
      # then it is assumed to be the same as ORB_AUTH_TOKENS.
      - ORB_CLIENT_AUTH_TOKENS=admin=ADMIN_TOKEN,read=READ_TOKEN
      # FOLLOW_AUTH_POLICY indicates whether a 'Follow' request is automatically accepted by this service (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - FOLLOW_AUTH_POLICY=accept-all
      # INVITE_WITNESS_AUTH_POLICY indicates whether an 'Invite' witness request is automatically accepted (accept-all policy)
      # or if the requesting service needs to be in an 'accept list' (accept-list policy).
      - INVITE_WITNESS_AUTH_POLICY=accept-all
      # TASK_MANAGER_CHECK_INTERVAL is the interval in which to check for scheduled tasks to be run. Note that this value is
      # merely the 'check' interval since each task has its own interval.
      # Default value: 10s.
      - TASK_MANAGER_CHECK_INTERVAL=3s
      # ANCHOR_EVENT_SYNC_INTERVAL is the interval in which anchor events are synchronized with other services that
      # we're following.
      # Default value: 1m.
      - ANCHOR_EVENT_SYNC_INTERVAL=10s
      # ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE is the minimum age of an activity to be synchronized. The activity will be processed
      # only if its age is greater than this value.
      # Default value: 1m
      - ANCHOR_EVENT_SYNC_MIN_ACTIVITY_AGE=20s
      # WITNESS_POLICY_CACHE_EXPIRATION sets the expiration time of witness policy cache.
      # Default value: 30s. Set the cache expiration very low since we're updating the policy frequently during the test.
      - WITNESS_POLICY_CACHE_EXPIRATION=500ms
      - ORB_REQUEST_TOKENS=vct-read=vctread,vct-write=vctwrite
    ports:
      - 49026:443
      - 49027:49027
    command: start
    volumes:
      - ./keys/tls:/etc/orb/tls
      - ./testdata/keys/domain2:/etc/orb/activitypub
    depends_on:
      - ipfs
      - orb5.kms
      - mongodb.domain5.com
      - orb.mq.domain5.com
    networks:
      orb_net:
        ipv4_address: 172.20.0.31

  ipfs:
    container_name: ipfs
    #TODO: this image doesn't work on ARM64 yet (investigate images)
    image: ipfs/go-ipfs:v0.8.0
    ports:
      - "4001:4001"
      - "4001:4001/udp"
      - "127.0.0.1:8080:8080"
      - "127.0.0.1:5001:5001"
    volumes:
      - ./data/ipfs:/data/ipfs
      - ./export:/export
    networks:
      orb_net:
        ipv4_address: 172.20.0.8

  mongodb.shared.com:
    container_name: mongodb.shared.com
    image: ${MONGODB_IMAGE}:${MONGODB_IMAGE_TAG}
    ports:
      - "27017:27017"
    networks:
      orb_net:
        ipv4_address: 172.20.0.9

  mongodb.domain5.com:
    container_name: mongodb.domain5.com
    image: ${MONGODB_IMAGE}:${MONGODB_IMAGE_TAG}
    ports:
      - "28017:27017"
    networks:
      orb_net:
        ipv4_address: 172.20.0.26

  orb.postgres:
    container_name: orb.postgres
    image: postgres:13
    restart: always
    environment:
      - POSTGRES_PASSWORD=${PGPASSWORD}
      - POSTGRES_DB=${PGDATABASE}
    ports:
      - 5432:5432
    networks:
      orb_net:
        ipv4_address: 172.20.0.10

  orb.trillian.log.server:
    container_name: orb.trillian.log.server
    image: ghcr.io/trustbloc/vct-log-server:v1.0.0-rc2
    restart: always
    command:
      - --quota_system=noop
      - --storage_system=postgres
      - --pg_conn_str=user=${PGUSER} host=orb.postgres password=${PGPASSWORD} dbname=${PGDATABASE} port=5432 sslmode=disable
      - --rpc_endpoint=172.20.0.11:8090
      - --logtostderr
    depends_on:
      - orb.postgres
    networks:
      orb_net:
        ipv4_address: 172.20.0.11
    ports:
      - 8090:8090

  orb.trillian.log.signer:
    container_name: orb.trillian.log.signer
    image: ghcr.io/trustbloc/vct-log-signer:v1.0.0-rc2
    restart: always
    command:
      - --quota_system=noop
      - --storage_system=postgres
      - --pg_conn_str=user=${PGUSER} host=orb.postgres password=${PGPASSWORD} dbname=${PGDATABASE} port=5432 sslmode=disable
      - --http_endpoint=172.20.0.12:8091
      - --force_master=true
      - --logtostderr
    depends_on:
      - orb.postgres
    networks:
      orb_net:
        ipv4_address: 172.20.0.12
    ports:
      - 8091:8091

  orb.kms:
    container_name: orb.kms
    restart: always
    image: ghcr.io/trustbloc/kms:v0.1.8
    environment:
      - KMS_BASE_URL=http://orb.kms:7878
      - KMS_HOST=172.20.0.13:7878
      - KMS_DATABASE_TYPE=mongodb
      - KMS_DATABASE_URL=mongodb://mongodb.shared.com:27017
      - KMS_DATABASE_PREFIX=keystore_
      - KMS_SECRET_LOCK_TYPE=local
      - KMS_SECRET_LOCK_KEY_PATH=/etc/kms/secret-lock.key
      - KMS_LOG_LEVEL=debug
    ports:
      - 7878:7878
    volumes:
      - ./keys/kms:/etc/kms
    depends_on:
      - mongodb.shared.com
    command: start
    networks:
      orb_net:
        ipv4_address: 172.20.0.13

  orb5.kms:
    container_name: orb5.kms
    restart: always
    image: ghcr.io/trustbloc/kms:v0.1.8
    environment:
      - KMS_BASE_URL=http://orb5.kms:7878
      - KMS_HOST=172.20.0.27:7878
      - KMS_DATABASE_TYPE=mongodb
      - KMS_DATABASE_URL=mongodb://mongodb.domain5.com:27017
      - KMS_DATABASE_PREFIX=keystore_
      - KMS_SECRET_LOCK_TYPE=local
      - KMS_SECRET_LOCK_KEY_PATH=/etc/kms/secret-lock.key
      - KMS_LOG_LEVEL=debug
    ports:
      - 7978:7878
    volumes:
      - ./keys/kms:/etc/kms
    depends_on:
      - mongodb.domain5.com
    command: start
    networks:
      orb_net:
        ipv4_address: 172.20.0.27

  orb.vct:
    container_name: orb.vct
    image: ghcr.io/trustbloc/vct:v1.0.0-rc2
    restart: always
    environment:
      - VCT_API_HOST=172.20.0.14:8077
      - VCT_LOGS=maple2020:rw@orb.trillian.log.server:8090,maple2022:rw@orb.trillian.log.server:8090
      - VCT_KMS_TYPE=aws
      - VCT_KMS_ENDPOINT=http://aws-kms:8020
      - VCT_LOG_SIGN_ACTIVE_KEY_ID=aws-kms://arn:aws:kms:ca-central-1:111122223333:alias/log-sign
      - AWS_ACCESS_KEY_ID=mock
      - AWS_SECRET_ACCESS_KEY=mock
      - VCT_TLS_SYSTEMCERTPOOL=true
      - VCT_DSN=postgres://postgres:${PGPASSWORD}@orb.postgres:5432
      - VCT_DATABASE_PREFIX=vct
      - VCT_API_READ_TOKEN=vctread
      - VCT_API_WRITE_TOKEN=vctwrite
      - VCT_TLS_CACERTS=/etc/orb/tls/ec-cacert.pem
      - VCT_CONTEXT_PROVIDER_URL=https://orb.file-server.com:12096/ld-contexts.json
    volumes:
      - ./keys/tls:/etc/orb/tls
    ports:
      - 8077:8077
    depends_on:
      - mongodb.shared.com
    command: start
    networks:
      orb_net:
        ipv4_address: 172.20.0.14

  orb.driver:
    container_name: orb.driver
    image: ghcr.io/trustbloc/orb-did-driver:latest
    environment:
      - ORB_DRIVER_HOST_URL=172.20.0.15:8070
      - ORB_DRIVER_DOMAIN=https://orb.domain3.com
      - ORB_DRIVER_TLS_SYSTEMCERTPOOL=true
      - ORB_DRIVER_TLS_CACERTS=/etc/orb/tls/ec-cacert.pem
      - ORB_DRIVER_SIDETREE_TOKEN=READ_TOKEN
      - ORB_DRIVER_VERIFY_RESOLUTION_RESULT_TYPE=all
    volumes:
      - ./keys/tls:/etc/orb/tls
    ports:
      - 8070:8070
    command: start
    networks:
      orb_net:
        ipv4_address: 172.20.0.15

  uni-resolver-web:
    image: universalresolver/uni-resolver-web:0.3.0
    ports:
      - "8062:8080"
    volumes:
      - ./uni-resolver-web/config.json:/opt/uni-resolver-java/uni-resolver-web/config.json
    networks:
      orb_net:
        ipv4_address: 172.20.0.16

  orb.mq.domain1.com:
    container_name: orb.mq.domain1.com
    image: rabbitmq:3-management-alpine
    environment:
      - CONFIG_FILE=/etc/rabbitmq/ext-config/rabbitmq.conf
    volumes:
      - ./rabbitmq-config/rabbitmq.conf:/etc/rabbitmq/ext-config/rabbitmq.conf
    ports:
      - 5672:5672
      # Management port
      - 15672:15672
    restart: unless-stopped
    networks:
      orb_net:
        ipv4_address: 172.20.0.17

  orb.mq.domain2.com:
    container_name: orb.mq.domain2.com
    image: rabbitmq:3-management-alpine
    environment:
      - CONFIG_FILE=/etc/rabbitmq/ext-config/rabbitmq.conf
    volumes:
      - ./rabbitmq-config/rabbitmq.conf:/etc/rabbitmq/ext-config/rabbitmq.conf
    ports:
      - 5682:5672
      # Management port
      - 15682:15672
    restart: unless-stopped
    networks:
      orb_net:
        ipv4_address: 172.20.0.18

  orb.mq.domain3.com:
    container_name: orb.mq.domain3.com
    image: rabbitmq:3-management-alpine
    environment:
      - CONFIG_FILE=/etc/rabbitmq/ext-config/rabbitmq.conf
    volumes:
      - ./rabbitmq-config/rabbitmq.conf:/etc/rabbitmq/ext-config/rabbitmq.conf
    ports:
      - 5692:5672
      # Management port
      - 15692:15672
    restart: unless-stopped
    networks:
      orb_net:
        ipv4_address: 172.20.0.19

  orb.mq.domain4.com:
    container_name: orb.mq.domain4.com
    image: rabbitmq:3-management-alpine
    environment:
      - CONFIG_FILE=/etc/rabbitmq/ext-config/rabbitmq.conf
    volumes:
      - ./rabbitmq-config/rabbitmq.conf:/etc/rabbitmq/ext-config/rabbitmq.conf
    ports:
      - 5694:5672
      # Management port
      - 15694:15672
    restart: unless-stopped
    networks:
      orb_net:
        ipv4_address: 172.20.0.20

  orb.mq.domain5.com:
    container_name: orb.mq.domain5.com
    image: rabbitmq:3-management-alpine
    environment:
      - CONFIG_FILE=/etc/rabbitmq/ext-config/rabbitmq.conf
    volumes:
      - ./rabbitmq-config/rabbitmq.conf:/etc/rabbitmq/ext-config/rabbitmq.conf
    ports:
      - 5695:5672
      # Management port
      - 15695:15672
    restart: unless-stopped
    networks:
      orb_net:
        ipv4_address: 172.20.0.25

  metrics.example.com:
    container_name: metrics.example.com
    image: prom/prometheus
    ports:
      - 9095:9090
    volumes:
      - ./prometheus-config/prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      orb_net:
        ipv4_address: 172.20.0.21

  orb.file-server.com: # file server for hosting static resources (e.g. JSON-LD contexts)
    container_name: orb.file-server.com
    image: halverneus/static-file-server:latest
    environment:
      - PORT=12096
      - FOLDER=/data
      - TLS_CERT=/etc/tls/ec-pubCert.pem
      - TLS_KEY=/etc/tls/ec-key.pem
      - CORS=true
    ports:
      - 12096:12096
    volumes:
      - ./contexts:/data
      - ./keys/tls:/etc/tls
    networks:
      orb_net:
        ipv4_address: 172.20.0.22


  aws-kms:
    container_name: aws-kms
    image: nsmithuk/local-kms:latest
    environment:
      - PORT=8020
      - KMS_SEED_PATH=/init/seed.yaml
      - KMS_REGION=ca-central-1
      - KMS_ACCOUNT_ID=111122223333
    ports:
      - 8020:8020
    entrypoint: "local-kms"
    volumes:
      - ./aws-config/init:/init/
    networks:
      orb_net:
        ipv4_address: 172.20.0.24

networks:
  orb_net:
    ipam:
      config:
        - subnet: 172.20.0.0/24
    driver: bridge
